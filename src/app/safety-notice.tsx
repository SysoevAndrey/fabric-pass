/**
 * The first line under the page heading, in every state — answers "what
 * does signing in commit me to" before a new visitor reaches the detailed
 * breakdown further down in <Collected />.
 */
export function SafetyNotice() {
  return <p className="safety-notice">This system asks for no permissions — only public profile information.</p>
}
